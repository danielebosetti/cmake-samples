

# install cmake-config + headers + lib to a folder: works
# use find_package, locate the cmake-config, import the headers and lib: works

# wip: use the vcpkg install scripts
# wip: find_package using a plain, cmake+vcpkg module
# 
